<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
<!--        <item name="colorPrimary">@color/colorPrimary</item>-->
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorPrimary">@color/rojoJuntaCyL</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@color/black</item>
    </style>

    <!--aqui podemos  añadir los distintos temas que queramos utilizar
    asi como personalizarlos a nuesto gusto-->

    <style name="dark" parent="Theme.AppCompat"></style>
    <style name="light" parent="Theme.AppCompat.Light"></style>

    <!--los estilos se agregan en androidManifest.xml-->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>